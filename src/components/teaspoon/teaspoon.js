import React from 'react';

/**
 * Render a teaspoon.
 *
 * @param {*} props unused for now.
 * @returns a div containing a teaspoon rendering.
 */
const Teaspoon = (props) => {

  return (
    <div className="teaspoon">
      {/* TBD teaspoon svg */}
      Pretend this is a teaspoon
    </div>
  )
}

export default Teaspoon;